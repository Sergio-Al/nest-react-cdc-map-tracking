<?xml version='1.0' encoding='UTF-8'?>

<!DOCTYPE properties SYSTEM 'http://java.sun.com/dtd/properties.dtd'>
<properties>

    <!-- ─── Database ──────────────────────────────────────── -->
    <entry key='database.driver'>org.postgresql.Driver</entry>
    <entry key='database.url'>jdbc:postgresql://traccar-db:5432/traccar</entry>
    <entry key='database.user'>traccar</entry>
    <entry key='database.password'>traccar_secret</entry>

    <!-- ─── Server ────────────────────────────────────────── -->
    <entry key='web.port'>8082</entry>

    <!-- ─── Logging ───────────────────────────────────────── -->
    <entry key='logger.enable'>true</entry>
    <entry key='logger.level'>info</entry>

    <!-- ─── GPS Protocol Ports ────────────────────────────── -->
    <!-- Default Traccar protocol (osmand) on TCP/UDP 5055 -->
    <entry key='osmand.port'>5055</entry>
    <!-- GPS103 / TK103 protocol -->
    <entry key='gps103.port'>5001</entry>

    <!-- ─── Webhook: Forward positions to NestJS ─────────── -->
    <entry key='forward.enable'>true</entry>
    <entry key='forward.url'>http://tracking-service:3000/api/traccar/positions</entry>
    <entry key='forward.json'>true</entry>

    <!-- ─── Event forwarding ─────────────────────────────── -->
    <entry key='event.forward.enable'>true</entry>
    <entry key='event.forward.url'>http://tracking-service:3000/api/traccar/events</entry>

    <!-- ─── Geocoder (optional – Nominatim / OSM) ─────────  -->
    <entry key='geocoder.enable'>false</entry>

    <!-- ─── Filtering ─────────────────────────────────────── -->
    <entry key='filter.enable'>true</entry>
    <entry key='filter.duplicate'>true</entry>
    <entry key='filter.zero'>true</entry>
    <entry key='filter.distance'>10</entry>

</properties>
